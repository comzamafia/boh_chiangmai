{
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/inventory-digest-daily",  "schedule": "0 1 * * *"  },
    { "path": "/api/cron/inventory-digest-weekly", "schedule": "0 1 * * 1"  }
  ]
}
